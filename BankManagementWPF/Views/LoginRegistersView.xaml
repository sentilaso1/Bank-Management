<UserControl x:Class="BankManagementSystem.WPF.Views.LoginRegistersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Login Registers" FontSize="20" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>
            <DataGrid x:Name="LoginRegistersDataGrid" Grid.Row="1" AutoGenerateColumns="True"
                      CanUserAddRows="False" CanUserDeleteRows="False"/>
        </Grid>
    </ScrollViewer>
</UserControl>
